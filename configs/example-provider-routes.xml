<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:camel="http://camel.apache.org/schema/blueprint"
  xsi:schemaLocation="
 http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
 http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

  <!-- Define TLS configuration for data provider -->
  <camel:sslContextParameters id="clientSslContext">
    <camel:keyManagers keyPassword="password">
      <camel:keyStore resource="etc/provider-keystore.p12" password="password"/>
    </camel:keyManagers>
    <camel:trustManagers>
      <camel:keyStore resource="etc/truststore.p12" password="password"/>
    </camel:trustManagers>
  </camel:sslContextParameters>

  <!-- Camel parsing configuration for json-->
  <camel:dataFormats>
    <camel:json id="jack" library="Jackson" prettyPrint="true"/>
  </camel:dataFormats>

  <!-- Routes -->
  <camelContext xmlns="http://camel.apache.org/schema/blueprint">

    <!-- Define first route that retrieves AI Tech Data and forwards it over secure IDS 
          commonunication protocol(IDSCP2) -->
    <route id="sendAITechData">

      <!-- Read the csv file -->
      <from uri="file://deploy/?fileName=ai_tech_data.csv&amp;charset=utf-8&amp;noop=true"/>
      
      <!-- Parses CSV -->
      <unmarshal>
        <csv delimiter="," useMaps="true"/>
      </unmarshal>
      
      <!-- Serialize data to JSON -->
      <marshal>
        <json/>
      </marshal>

      <!-- Forward to secure container "consumer core" through  IDS communication protocol (IDSCP2) -->
      <to uri="idscp2client://consumer-core:29292?awaitResponse=true&amp;sslContextParameters=#clientSslContext&amp;useIdsMessages=false"/>
      
      <!-- Sets the content type -->
      <setHeader name="Content-Type">
        <constant>application/json</constant>
      </setHeader>

    </route>

    <!-- Define Second route that retrieves Farmer Data and forwards it over secure IDS 
          commonunication protocol(IDSCP2) -->
    <route id="sendFarmerData">

      <!-- Read the csv file -->
      <from uri="file://deploy/?fileName=farmer_data.csv&amp;charset=utf-8&amp;noop=true"/>
      
      <!-- Parses CSV -->
      <unmarshal>
        <csv delimiter="," useMaps="true"/>
      </unmarshal>
      
      <!-- Serializes data to JSON -->
      <marshal>
        <json/>
      </marshal>

      <to uri="idscp2client://consumer-core:29292?awaitResponse=true&amp;sslContextParameters=#clientSslContext&amp;useIdsMessages=false"/>
      
      <!-- Sets the content type -->
      <setHeader name="Content-Type">
        <constant>application/json</constant>
      </setHeader>

    </route>

  </camelContext>
</blueprint>
